useDynLib(deSolve)

export(aquaphy, ccl4model, daspk, lsoda, lsodar, lsode, lsodes,
       ode, ode.1D, ode.2D, ode.band, rk4, vode, diagnostics)

export(rk, euler, rkMethod)

export(rk_c)            # test version; will be removed later

export(rkAuto, rkFixed) # exported but marked as "internal" in .Rd

